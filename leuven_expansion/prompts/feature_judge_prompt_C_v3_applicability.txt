Judge this target_word x feature pair as a Leuven applicability rater.

The Leuven applicability matrix was built by asking 4 independent raters per cell: "Does this feature characterize this word? Mark 1 (yes) or 0 (no)."

Your output (0–4) is the predicted sum of those 4 ratings.

Ask yourself these questions in order:

1. Is this feature an ordinary, normal property of the word — something a typical person would readily agree with when asked?
   -> If yes for all 4 raters: assign 4
   -> If yes for most (3 of 4): assign 3
   -> If yes for about half: assign 2
   -> If yes for only some: assign 1

2. Is this feature only technically true in a trivial sense (has mass, exists, is made of particles), or is it true only rarely, metaphorically, or in exceptional contexts?
   -> If so, assign 0 regardless of technical truth

3. Is this feature absent only because participants might not spontaneously generate it, even though normal people would clearly agree it characterizes the word?
   -> Do NOT reduce the score for this reason alone

Important: The Leuven applicability task fills in connections that free generation misses. A feature can deserve a high value (3 or 4) even if it would not appear in free generation of features for that word, as long as typical people would endorse it when asked.

Use 0 only for features that are irrelevant, trivially universal, purely metaphorical, or that most raters would genuinely reject.

Judge only this one pair. Do not use surrounding context. Return exactly one JSON object with no markdown or text outside it.
The JSON must include exactly these fields: target_word, feature_id, feature_value, confidence, ambiguous, reason.
Respond in English only. DO NOT use any non-English characters. Keep your reason to one concise sentence (maximum 150 characters).
