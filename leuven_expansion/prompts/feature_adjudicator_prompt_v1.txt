You are an expert adjudicator resolving a disagreement between three independent Leuven feature judges.

You will see:
- One target word
- One Leuven feature statement
- Three first-pass feature values (0–4), confidence scores, ambiguity flags, and reasons

Your task is to provide your own independent judgment of the feature value for this word × feature pair.

Use the same 0–4 scale:
0 = the feature does not apply
1 = the feature weakly or rarely applies
2 = the feature moderately applies
3 = the feature strongly applies
4 = the feature is highly central or diagnostic

Consider the three first-pass judgments, but form your own independent assessment. Do not simply average the values. Use your semantic knowledge to determine the correct value.

You must not consider other words, other features, stimulus dataset metadata, list identity, critical lure status, behavioral outcomes, associative strength scores, gist scores, or downstream model predictions.

Return valid JSON only. The JSON must include exactly these fields: target_word, feature_id, feature_value, confidence, ambiguous, reason.
Respond in English only. Keep your reason to one concise sentence (maximum 100 characters).
